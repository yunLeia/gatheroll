name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: apps/web/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build

  api:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: gatheroll
          POSTGRES_PASSWORD: gatheroll
          POSTGRES_DB: gatheroll_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U gatheroll -d gatheroll_test"
          --health-interval 5s --health-timeout 5s --health-retries 10
    env:
      DATABASE_URL: postgresql+psycopg://gatheroll:gatheroll@localhost:5432/gatheroll_test
      TEST_DATABASE_URL: postgresql+psycopg://gatheroll:gatheroll@localhost:5432/gatheroll_test
    defaults:
      run:
        working-directory: apps/api
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install -e '.[dev]'
      - run: alembic upgrade head
      - run: alembic check
      - run: ruff check .
      - run: mypy src tests
      - run: pytest
